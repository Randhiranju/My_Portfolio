1. run  to create  react app 
       npm create vite@latest ./ -- --template react 
2. Tailwind css installation and init
3. Package installation using 
    npm install --legacy-peer-deps @react-three/fiber @react-three/drei maath react-tilt react-vertical-timeline-component @emailjs/browser framer-motion react-router-dom  

4. logo can be generated from logo.com

5. to make css work with vite 
         npm install --legacy-peer-deps -D postcss autoprefixer 
         npx tailwindcss init -p
6. unicode for space   &nbsp   

7. we can't use three/fibre without three js set up 
run below command in terminal
        npm install --legacy-peer-deps three

8. Logo can be designed from logo.com
9. 3D models can be downloaded from online websites
    for example: Sketchfab  
           click on downloadables download and use the models